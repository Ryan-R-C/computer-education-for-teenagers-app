import { ButtonProps } from '../../../types'
import * as S from './styled'

export default function CardContainer({ children }: ButtonProps) {
  return <S.CardContainer onClick={() => {}}>{children}</S.CardContainer>
}
