import { ReactProps } from '../../../../types'
import * as S from './styled'

export default function FooterBarItem({ children }: ReactProps) {
  return <S.FooterBarItem>{children}</S.FooterBarItem>
}
